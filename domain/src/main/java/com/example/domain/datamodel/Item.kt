package com.example.domain.datamodel

import com.example.domain.constants.RepositoryConstants

/**
 * ATTENTION!
 * Field names need to match the constants in [RepositoryConstants].
 *
 * @param id Generated by Firebase,
 * thus it is not known when a Item is first created.
 */
data class Item(val title: String,
                val position: Int,
                val userListId: String,
                val id: String = "") {
    /**
     * Required by Firebase Firestore.
     */
    constructor() : this("", -1, "")
}