package com.example.david.lists.data.datamodel

import com.example.david.lists.data.remote.RemoteRepositoryConstants

/**
 * ATTENTION!
 * Field names need to match the constants in [RemoteRepositoryConstants].
 *
 * @param id Generated by Firebase,
 * thus it is not known when a Item is first created.
 */
data class Item(val title: String,
                val position: Int,
                val userListId: String,
                val id: String = "") {
    /**
     * Required by Firebase Firestore.
     */
    constructor() : this("", -1, "")
}
